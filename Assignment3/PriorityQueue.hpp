#ifndef PriorityQueue_hpp
#define PriorityQueue_hpp

#include <stdio.h>

class PriorityQueue
{
private:
    struct elements
    {
        char value;
        int count;
    };
    int elementCount;
    int MAX_SIZE;
    elements* a;
public:
    
};

#endif /* PriorityQueue_hpp */
